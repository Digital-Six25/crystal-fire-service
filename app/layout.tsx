import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navbar from "@/components/navbar"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Crystal Fire Services - Professional Fire Protection & Certification",
  description:
    "Professional fire protection services including installation, testing, maintenance and certification for commercial, residential & industrial premises in Australia.",
  keywords: "fire protection, fire safety, AFSS, certification, fire systems, Australia",
  authors: [{ name: "Crystal Fire Services" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  )
}
